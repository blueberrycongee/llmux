# 高级 Agent 记忆系统 (Advanced Agent Memory System)

`internal/memory` 包为 LLMux Agent 提供了一个复杂的多层记忆系统，旨在模拟类人的回忆和学习能力。它超越了简单的消息历史记录，使 Agent 能够长期存储、管理和检索结构化知识。

受 [Mem0](https://github.com/mem0ai/mem0) 启发，该系统实现了具有智能摄入和混合检索功能的分层架构。

## 🧠 核心架构

该系统由 `MemoryManager` 编排，分为三个主要层级：

1.  **短期记忆 (Session)**
    *   **目的**: 维护当前的对话上下文。
    *   **机制**: 存储原始的 `ChatMessage` 历史记录。
    *   **存储**: 内存或 Redis (通过 `SessionStore` 接口)。

2.  **长期记忆 (Vector / RAG)**
    *   **目的**: 存储事实性知识、用户偏好和过往经历。
    *   **机制**: 使用向量嵌入 (Embeddings) 和混合相似度搜索。
    *   **存储**: 向量数据库 (通过 `VectorStore` 接口)。

3.  **实体记忆 (Structured)**
    *   **目的**: 追踪实体 (用户、Agent) 的特定属性。
    *   **机制**: 附加在记忆条目上的结构化元数据。

## ✨ 关键特性

### 1. 智能摄入 (Smart Ingestion)
系统不仅仅是存储原始文本，而是利用 LLM 从用户输入中 **提取** 有意义的事实。
*   **提取**: 识别核心事实、实体和类别 (例如 "hobbies", "work")。
*   **动作决策**: LLM 决定是执行 `ADD` (新增)、`UPDATE` (更新) 还是 `DELETE` (删除) 操作。

### 2. 动态冲突解决 (Dynamic Resolution)
系统自动处理矛盾和更新：
*   **去重**: 使用余弦相似度 (阈值 `>0.9`) 检测“新”事实是否只是现有事实的重述。
*   **决议**:
    *   **ADD**: 如果不存在冲突，则添加新信息。
    *   **UPDATE**: 用新细节替换旧信息 (例如，“我搬到了柏林” 替换 “我住在巴黎”)。
    *   **DELETE**: 根据请求删除特定记忆 (例如，“忘掉我的电话号码”)。

### 3. 混合检索 (Hybrid Retrieval)
检索不仅仅是匹配关键词。我们使用混合评分公式来呈现最相关 *且* 最新的信息：

$$ Score = (SemanticScore \times 0.8) + (RecencyScore \times 0.2) $$

*   **语义评分 (Semantic Score)**: 查询向量与记忆向量之间的余弦相似度。
*   **时效评分 (Recency Score)**: 指数衰减函数 `exp(-0.01 * hours_passed)`，优先考虑较新的记忆。

## 🚀 使用指南

### 初始化

```go
import (
    "github.com/blueberrycongee/llmux/internal/memory"
    "github.com/blueberrycongee/llmux/internal/memory/inmem"
)

// 1. 初始化组件
sessionStore := inmem.NewInMemorySessionStore()
vectorStore := inmem.NewMemoryVectorStore() // 或 Qdrant/Milvus
llmClient := inmem.NewRealLLMClientSimulator() // 或 OpenAI Client
embedder := &inmem.SimpleEmbedder{} // 或 OpenAI Embedder

// 2. 创建管理器
memManager := memory.NewMemoryManager(sessionStore, vectorStore, embedder, llmClient)
```

### 摄入记忆 (智能模式)

将原始用户输入传递给 `IngestMemory`。系统将分析它并更新状态。

```go
ctx := context.Background()
userID := "user_123"
userInput := "我刚搬到东京，现在很喜欢吃寿司。"

// 提取: "User lives in Tokyo", "User likes sushi"
// 动作: ADD / UPDATE
err := memManager.IngestMemory(ctx, userID, userInput)
```

### 检索上下文

生成回复时，获取相关上下文：

```go
query := "晚饭我该吃什么？"
contextStr, err := memManager.RetrieveRelevantContext(ctx, query, userID)

// 结果: "User likes sushi. User lives in Tokyo."
```

## 🛠 配置

该系统高度模块化。你可以通过实现以下接口来使用自己的后端：

*   **`VectorStore`**: 用于存储嵌入 (`Search`, `Add`, `Delete`)。
*   **`LLMClient`**: 用于提取/决议步骤 (`Call`)。
*   **`Embedder`**: 用于生成向量嵌入 (`Embed`)。
*   **`SessionStore`**: 用于原始聊天记录。

## 🧪 测试 (Testing)

本模块采用了一种综合测试策略，优先考虑 **确定性 (Determinism)** 和 **真实逻辑路径**，而不是简单的 Mock。我们使用 `RealLLMClientSimulator` 来解析 Prompt 并执行预定义的逻辑规则，而不是 Mock 返回值。

运行测试：
```bash
go test -v ./internal/memory/tests/...
```

### 测试用例详解 (Test Case Breakdown)

我们将所有关键测试合并到了 `manager_test.go` 中，以覆盖系统的 5 个核心支柱：

| 测试函数名 | 描述 | 关键验证点 |
| :--- | :--- | :--- |
| **`TestManager_SessionLifecycle`** | 验证短期记忆操作。 | 确保 Session 可以被创建、追加消息，并且历史记录可以被完整读取。 |
| **`TestManager_VectorRetrieval`** | 验证长期记忆隔离。 | 确保用户 A 无法检索到用户 B 的记忆（多租户数据隔离）。 |
| **`TestManager_HybridRetrieval_Recency`** | 验证混合评分公式。 | 摄入两个完全相同的事实（一个旧，一个新）。验证由于时间衰减，**较新的事实排名更高**。 |
| **`TestManager_SmartIngestion`** | 验证 LLM 驱动的提取。 | 输入：*"我搬到了柏林并且喜欢吃咖喱香肠"*。验证系统是否提取并存储了 **两个独立的事实** ("User lives in Berlin", "User likes currywurst")。 |
| **`TestManager_MemoryResolution`** | 验证动态冲突解决。 | 输入：*"忘掉我喜欢 Java 这件事"*。验证模拟器是否触发了 `DELETE` 动作，并且该记忆确实从存储中被移除了。 |

### 为什么使用模拟器 (Simulator)?

我们使用 `RealLLMClientSimulator` 的原因：
1.  **速度**: 无网络延迟（毫秒级运行）。
2.  **成本**: 零 API 调用成本。
3.  **可靠性**: 避免了 LLM 输出不确定性导致的测试抖动 (Flaky Tests)。
4.  **逻辑验证**: 与简单的 Mock 不同，它验证了 `Manager` 是否正确构建了 Prompt，以及是否能正确处理 LLM 返回的 JSON 结构和 Action 逻辑。
